using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DemoUILightsCounter : MonoBehaviour
{

    private void Start()
    {

    }

    // Update is called once per frame
    /*
    void Update()
    {
        try
        {
            if (playerData.lightCurrency > 0)
            {
                gameObject.GetComponent<Text>().text = "Lights: " + playerData.lightCurrency.ToString();
            }

        }
        catch
        {

        }
    }
    */
}
